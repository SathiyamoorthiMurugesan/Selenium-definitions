What is the difference between @FindBy & @FindAll in page factory?
Can we handle dynamic xpath or any dynamic loaded value in Page Factory design pattern?
How will you open a particular link in a separate window(new window)?
What are the three tags that are used to put the dependency in pom.xml?

https://www.techbeamers.com/websites-to-practice-selenium-webdriver-online/


How can we add file in .gitignore to avoid the file get checked in Git Hub?
How can we pass the string from test case & identify the WebElement?
Asssume there are two html tags for the same element. We can say those are duplicate,. how will you highlight the active WebElement using xpath ?
//div[contains(@class,'Active')]//(following the WebElement xpath)
What are the limitations of Page Factory Model ? Why you still prefer it ?






